import * as path from 'path';
import { readFile } from "../Day1/readFile";

const file = readFile(
    path.join(
      __dirname, 
      '../../adventOfCode/Day4/inputs/testInput.txt'
    )
  );

const a = 5;